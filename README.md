
# CertifyPro Upgraded Website

## Setup
1. Replace Firebase config in `pages/index.js` with your project credentials.
2. Install dependencies: `npm install`
3. Run locally: `npm run dev`
4. Deploy: Push to GitHub and connect to Vercel for live site.
